<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="/styles/css/style.css" />
    <link rel="icon" type="image/x-icon" href="/favicon/favicon.ico" />

    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <title>Login</title>
  </head>

  <body>
    <div class="login-signup-page">
      <div class="login__container">
        <div class="login__container-head">
          <h1>Login</h1>
          <p>Please enter your details to login</p>
        </div>
  
        <form
          method="POST"
          action="/user/login"
          class="login__container-body form__default"
        >
          <div class="form__default-body">
            <div class="form__default-element">
              <label for="email" class="form__default-label">Email</label>
              <input
                type="email"
                name="email"
                id="email"
                placeholder="john.doe@mail.xyz"
                class="form__default-input"
                required
              />
            </div>
            <div class="form__default-element">
              <label for="password" class="form__default-label">Password</label>
              <input
                type="password"
                name="password"
                id="password"
                placeholder="Enter your password"
                class="form__default-input"
                required
              />
            </div>
            <div class="form__default-element login-signup-form-button">
              <button class="button__default">Submit</button>
            </div>
          </div>
        </form>
        <div class="login__container-footer">
          <p>Don't have an account? <a href="/signup">Sign up</a></p>
        </div>
      </div>
    </div>

    <%- include('partials/toast') %>
  </body>
</html>
